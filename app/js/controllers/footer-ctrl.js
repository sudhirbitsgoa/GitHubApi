(function (angular) {
    "use strict";

    angular
        .module('kwik')
        /* Setup Layout Part - Header */
        .controller('FooterCtrl', ['$scope',
            function ($scope) {

                $scope.init = function () {
                };

            }]);
})(angular);